\doxysection{scikitplot.\+classifiers Namespace Reference}
\hypertarget{namespacescikitplot_1_1classifiers}{}\label{namespacescikitplot_1_1classifiers}\index{scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection*{Functions}
\begin{DoxyCompactItemize}
\item 
\mbox{\hyperlink{namespacescikitplot_1_1classifiers_a624273a2a8d222fd7720a11821b3340c}{classifier\+\_\+factory}} (clf)
\item 
\mbox{\hyperlink{namespacescikitplot_1_1classifiers_ac3fc1ae66eb376418b6387caeecd7ff1}{plot\+\_\+confusion\+\_\+matrix\+\_\+with\+\_\+cv}} (clf, X, y, labels=None, true\+\_\+labels=None, pred\+\_\+labels=None, title=None, normalize=False, hide\+\_\+zeros=False, x\+\_\+tick\+\_\+rotation=0, do\+\_\+cv=\mbox{\hyperlink{classTrue}{True}}, cv=None, shuffle=\mbox{\hyperlink{classTrue}{True}}, random\+\_\+state=None, ax=None, figsize=None, cmap=\textquotesingle{}Blues\textquotesingle{}, title\+\_\+fontsize="{}large"{}, text\+\_\+fontsize="{}medium"{})
\item 
\mbox{\hyperlink{namespacescikitplot_1_1classifiers_a5b1de56edca4dcb6404139ede4488de1}{plot\+\_\+roc\+\_\+curve\+\_\+with\+\_\+cv}} (clf, X, y, title=\textquotesingle{}ROC Curves\textquotesingle{}, do\+\_\+cv=\mbox{\hyperlink{classTrue}{True}}, cv=None, shuffle=\mbox{\hyperlink{classTrue}{True}}, random\+\_\+state=None, curves=(\textquotesingle{}micro\textquotesingle{}, \textquotesingle{}macro\textquotesingle{}, \textquotesingle{}each\+\_\+class\textquotesingle{}), ax=None, figsize=None, cmap=\textquotesingle{}nipy\+\_\+spectral\textquotesingle{}, title\+\_\+fontsize="{}large"{}, text\+\_\+fontsize="{}medium"{})
\item 
\mbox{\hyperlink{namespacescikitplot_1_1classifiers_ac00475e0b77fc0a5e917362da1a5c7ab}{plot\+\_\+ks\+\_\+statistic\+\_\+with\+\_\+cv}} (clf, X, y, title=\textquotesingle{}KS Statistic Plot\textquotesingle{}, do\+\_\+cv=\mbox{\hyperlink{classTrue}{True}}, cv=None, shuffle=\mbox{\hyperlink{classTrue}{True}}, random\+\_\+state=None, ax=None, figsize=None, title\+\_\+fontsize="{}large"{}, text\+\_\+fontsize="{}medium"{})
\item 
\mbox{\hyperlink{namespacescikitplot_1_1classifiers_a60991a3eaa90432a8a35e53f93c086e7}{plot\+\_\+precision\+\_\+recall\+\_\+curve\+\_\+with\+\_\+cv}} (clf, X, y, title=\textquotesingle{}Precision-\/Recall Curve\textquotesingle{}, do\+\_\+cv=\mbox{\hyperlink{classTrue}{True}}, cv=None, shuffle=\mbox{\hyperlink{classTrue}{True}}, random\+\_\+state=None, curves=(\textquotesingle{}micro\textquotesingle{}, \textquotesingle{}each\+\_\+class\textquotesingle{}), ax=None, figsize=None, cmap=\textquotesingle{}nipy\+\_\+spectral\textquotesingle{}, title\+\_\+fontsize="{}large"{}, text\+\_\+fontsize="{}medium"{})
\end{DoxyCompactItemize}


\doxysubsection{Function Documentation}
\Hypertarget{namespacescikitplot_1_1classifiers_a624273a2a8d222fd7720a11821b3340c}\index{scikitplot.classifiers@{scikitplot.classifiers}!classifier\_factory@{classifier\_factory}}
\index{classifier\_factory@{classifier\_factory}!scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection{\texorpdfstring{classifier\_factory()}{classifier\_factory()}}
{\footnotesize\ttfamily \label{namespacescikitplot_1_1classifiers_a624273a2a8d222fd7720a11821b3340c} 
scikitplot.\+classifiers.\+classifier\+\_\+factory (\begin{DoxyParamCaption}\item[{}]{clf}{}\end{DoxyParamCaption})}

\begin{DoxyVerb}Embeds scikit-plot instance methods in an sklearn classifier.

Args:
    clf: Scikit-learn classifier instance

Returns:
    The same scikit-learn classifier instance passed in **clf**
    with embedded scikit-plot instance methods.

Raises:
    ValueError: If **clf** does not contain the instance methods
        necessary for scikit-plot instance methods.
\end{DoxyVerb}
 

Definition at line \mbox{\hyperlink{classifiers_8py_source_l00024}{24}} of file \mbox{\hyperlink{classifiers_8py_source}{classifiers.\+py}}.

\Hypertarget{namespacescikitplot_1_1classifiers_ac3fc1ae66eb376418b6387caeecd7ff1}\index{scikitplot.classifiers@{scikitplot.classifiers}!plot\_confusion\_matrix\_with\_cv@{plot\_confusion\_matrix\_with\_cv}}
\index{plot\_confusion\_matrix\_with\_cv@{plot\_confusion\_matrix\_with\_cv}!scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection{\texorpdfstring{plot\_confusion\_matrix\_with\_cv()}{plot\_confusion\_matrix\_with\_cv()}}
{\footnotesize\ttfamily \label{namespacescikitplot_1_1classifiers_ac3fc1ae66eb376418b6387caeecd7ff1} 
scikitplot.\+classifiers.\+plot\+\_\+confusion\+\_\+matrix\+\_\+with\+\_\+cv (\begin{DoxyParamCaption}\item[{}]{clf}{, }\item[{}]{X}{, }\item[{}]{y}{, }\item[{}]{labels}{ = {\ttfamily None}, }\item[{}]{true\+\_\+labels}{ = {\ttfamily None}, }\item[{}]{pred\+\_\+labels}{ = {\ttfamily None}, }\item[{}]{title}{ = {\ttfamily None}, }\item[{}]{normalize}{ = {\ttfamily False}, }\item[{}]{hide\+\_\+zeros}{ = {\ttfamily False}, }\item[{}]{x\+\_\+tick\+\_\+rotation}{ = {\ttfamily 0}, }\item[{}]{do\+\_\+cv}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{cv}{ = {\ttfamily None}, }\item[{}]{shuffle}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{random\+\_\+state}{ = {\ttfamily None}, }\item[{}]{ax}{ = {\ttfamily None}, }\item[{}]{figsize}{ = {\ttfamily None}, }\item[{}]{cmap}{ = {\ttfamily \textquotesingle{}Blues\textquotesingle{}}, }\item[{}]{title\+\_\+fontsize}{ = {\ttfamily "{}large"{}}, }\item[{}]{text\+\_\+fontsize}{ = {\ttfamily "{}medium"{}}}\end{DoxyParamCaption})}

\begin{DoxyVerb}Generates the confusion matrix for a given classifier and dataset.

Args:
    clf: Classifier instance that implements ``fit`` and ``predict``
        methods.

    X (array-like, shape (n_samples, n_features)):
        Training vector, where n_samples is the number of samples and
        n_features is the number of features.

    y (array-like, shape (n_samples) or (n_samples, n_features)):
        Target relative to X for classification.

    labels (array-like, shape (n_classes), optional): List of labels to
        index the matrix. This may be used to reorder or select a subset of
        labels. If none is given, those that appear at least once in ``y``
        are used in sorted order.
        (new in v0.2.5)

    true_labels (array-like, optional): The true labels to display.
        If none is given, then all of the labels are used.

    pred_labels (array-like, optional): The predicted labels to display.
        If none is given, then all of the labels are used.

    title (string, optional): Title of the generated plot. Defaults to
        "Confusion Matrix" if normalize` is True. Else, defaults to
        "Normalized Confusion Matrix.

    normalize (bool, optional): If True, normalizes the confusion matrix
        before plotting. Defaults to False.

    hide_zeros (bool, optional): If True, does not plot cells containing a
        value of zero. Defaults to False.

    x_tick_rotation (int, optional): Rotates x-axis tick labels by the
        specified angle. This is useful in cases where there are numerous
        categories and the labels overlap each other.

    do_cv (bool, optional): If True, the classifier is cross-validated on
        the dataset using the cross-validation strategy in `cv` to generate
        the confusion matrix. If False, the confusion matrix is generated
        without training or cross-validating the classifier. This assumes
        that the classifier has already been called with its `fit` method
        beforehand.

    cv (int, cross-validation generator, iterable, optional): Determines
        the cross-validation strategy to be used for splitting.

        Possible inputs for cv are:
          - None, to use the default 3-fold cross-validation,
          - integer, to specify the number of folds.
          - An object to be used as a cross-validation generator.
          - An iterable yielding train/test splits.

        For integer/None inputs, if ``y`` is binary or multiclass,
        :class:`StratifiedKFold` used. If the estimator is not a classifier
        or if ``y`` is neither binary nor multiclass, :class:`KFold` is
        used.

    shuffle (bool, optional): Used when do_cv is set to True. Determines
        whether to shuffle the training data before splitting using
        cross-validation. Default set to True.

    random_state (int :class:`RandomState`): Pseudo-random number generator
        state used for random sampling.

    ax (:class:`matplotlib.axes.Axes`, optional): The axes upon which to
        plot the learning curve. If None, the plot is drawn on a new set of
        axes.

    figsize (2-tuple, optional): Tuple denoting figure size of the plot
        e.g. (6, 6). Defaults to ``None``.

    cmap (string or :class:`matplotlib.colors.Colormap` instance, optional):
        Colormap used for plotting the projection. View Matplotlib Colormap
        documentation for available options.
        https://matplotlib.org/users/colormaps.html

    title_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "large".

    text_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "medium".


Returns:
    ax (:class:`matplotlib.axes.Axes`): The axes on which the plot was
        drawn.

Example:
    >>> rf = classifier_factory(RandomForestClassifier())
    >>> rf.plot_confusion_matrix(X, y, normalize=True)
    <matplotlib.axes._subplots.AxesSubplot object at 0x7fe967d64490>
    >>> plt.show()

    .. image:: _static/examples/plot_confusion_matrix.png
       :align: center
       :alt: Confusion matrix
\end{DoxyVerb}
 

Definition at line \mbox{\hyperlink{classifiers_8py_source_l00070}{70}} of file \mbox{\hyperlink{classifiers_8py_source}{classifiers.\+py}}.

\Hypertarget{namespacescikitplot_1_1classifiers_ac00475e0b77fc0a5e917362da1a5c7ab}\index{scikitplot.classifiers@{scikitplot.classifiers}!plot\_ks\_statistic\_with\_cv@{plot\_ks\_statistic\_with\_cv}}
\index{plot\_ks\_statistic\_with\_cv@{plot\_ks\_statistic\_with\_cv}!scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection{\texorpdfstring{plot\_ks\_statistic\_with\_cv()}{plot\_ks\_statistic\_with\_cv()}}
{\footnotesize\ttfamily \label{namespacescikitplot_1_1classifiers_ac00475e0b77fc0a5e917362da1a5c7ab} 
scikitplot.\+classifiers.\+plot\+\_\+ks\+\_\+statistic\+\_\+with\+\_\+cv (\begin{DoxyParamCaption}\item[{}]{clf}{, }\item[{}]{X}{, }\item[{}]{y}{, }\item[{}]{title}{ = {\ttfamily \textquotesingle{}KS~Statistic~Plot\textquotesingle{}}, }\item[{}]{do\+\_\+cv}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{cv}{ = {\ttfamily None}, }\item[{}]{shuffle}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{random\+\_\+state}{ = {\ttfamily None}, }\item[{}]{ax}{ = {\ttfamily None}, }\item[{}]{figsize}{ = {\ttfamily None}, }\item[{}]{title\+\_\+fontsize}{ = {\ttfamily "{}large"{}}, }\item[{}]{text\+\_\+fontsize}{ = {\ttfamily "{}medium"{}}}\end{DoxyParamCaption})}

\begin{DoxyVerb}Generates the KS Statistic plot for a given classifier and dataset.

Args:
    clf: Classifier instance that implements "fit" and "predict_proba"
        methods.

    X (array-like, shape (n_samples, n_features)):
        Training vector, where n_samples is the number of samples and
        n_features is the number of features.

    y (array-like, shape (n_samples) or (n_samples, n_features)):
        Target relative to X for classification.

    title (string, optional): Title of the generated plot. Defaults to
        "KS Statistic Plot".

    do_cv (bool, optional): If True, the classifier is cross-validated on
        the dataset using the cross-validation strategy in `cv` to generate
        the confusion matrix. If False, the confusion matrix is generated
        without training or cross-validating the classifier. This assumes
        that the classifier has already been called with its `fit` method
        beforehand.

    cv (int, cross-validation generator, iterable, optional): Determines
        the cross-validation strategy to be used for splitting.

        Possible inputs for cv are:
          - None, to use the default 3-fold cross-validation,
          - integer, to specify the number of folds.
          - An object to be used as a cross-validation generator.
          - An iterable yielding train/test splits.

        For integer/None inputs, if ``y`` is binary or multiclass,
        :class:`StratifiedKFold` used. If the estimator is not a classifier
        or if ``y`` is neither binary nor multiclass, :class:`KFold` is
        used.

    shuffle (bool, optional): Used when do_cv is set to True. Determines
        whether to shuffle the training data before splitting using
        cross-validation. Default set to True.

    random_state (int :class:`RandomState`): Pseudo-random number generator
        state used for random sampling.

    ax (:class:`matplotlib.axes.Axes`, optional): The axes upon which to
        plot the learning curve. If None, the plot is drawn on a new set of
        axes.

    figsize (2-tuple, optional): Tuple denoting figure size of the plot
        e.g. (6, 6). Defaults to ``None``.

    title_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "large".

    text_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "medium".

Returns:
    ax (:class:`matplotlib.axes.Axes`): The axes on which the plot was
        drawn.

Example:
        >>> lr = classifier_factory(LogisticRegression())
        >>> lr.plot_ks_statistic(X, y, random_state=1)
        <matplotlib.axes._subplots.AxesSubplot object at 0x7fe967d64490>
        >>> plt.show()

    .. image:: _static/examples/plot_ks_statistic.png
       :align: center
       :alt: KS Statistic
\end{DoxyVerb}
 

Definition at line \mbox{\hyperlink{classifiers_8py_source_l00352}{352}} of file \mbox{\hyperlink{classifiers_8py_source}{classifiers.\+py}}.

\Hypertarget{namespacescikitplot_1_1classifiers_a60991a3eaa90432a8a35e53f93c086e7}\index{scikitplot.classifiers@{scikitplot.classifiers}!plot\_precision\_recall\_curve\_with\_cv@{plot\_precision\_recall\_curve\_with\_cv}}
\index{plot\_precision\_recall\_curve\_with\_cv@{plot\_precision\_recall\_curve\_with\_cv}!scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection{\texorpdfstring{plot\_precision\_recall\_curve\_with\_cv()}{plot\_precision\_recall\_curve\_with\_cv()}}
{\footnotesize\ttfamily \label{namespacescikitplot_1_1classifiers_a60991a3eaa90432a8a35e53f93c086e7} 
scikitplot.\+classifiers.\+plot\+\_\+precision\+\_\+recall\+\_\+curve\+\_\+with\+\_\+cv (\begin{DoxyParamCaption}\item[{}]{clf}{, }\item[{}]{X}{, }\item[{}]{y}{, }\item[{}]{title}{ = {\ttfamily \textquotesingle{}Precision-\/Recall~Curve\textquotesingle{}}, }\item[{}]{do\+\_\+cv}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{cv}{ = {\ttfamily None}, }\item[{}]{shuffle}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{random\+\_\+state}{ = {\ttfamily None}, }\item[{}]{curves}{ = {\ttfamily (\textquotesingle{}micro\textquotesingle{},~\textquotesingle{}each\+\_\+class\textquotesingle{})}, }\item[{}]{ax}{ = {\ttfamily None}, }\item[{}]{figsize}{ = {\ttfamily None}, }\item[{}]{cmap}{ = {\ttfamily \textquotesingle{}nipy\+\_\+spectral\textquotesingle{}}, }\item[{}]{title\+\_\+fontsize}{ = {\ttfamily "{}large"{}}, }\item[{}]{text\+\_\+fontsize}{ = {\ttfamily "{}medium"{}}}\end{DoxyParamCaption})}

\begin{DoxyVerb}Generates the Precision-Recall curve for a given classifier and dataset.

Args:
    clf: Classifier instance that implements "fit" and "predict_proba"
        methods.

    X (array-like, shape (n_samples, n_features)):
        Training vector, where n_samples is the number of samples and
        n_features is the number of features.

    y (array-like, shape (n_samples) or (n_samples, n_features)):
        Target relative to X for classification.

    title (string, optional): Title of the generated plot. Defaults to
        "Precision-Recall Curve".

    do_cv (bool, optional): If True, the classifier is cross-validated on
        the dataset using the cross-validation strategy in `cv` to generate
        the confusion matrix. If False, the confusion matrix is generated
        without training or cross-validating the classifier. This assumes
        that the classifier has already been called with its `fit` method
        beforehand.

    cv (int, cross-validation generator, iterable, optional): Determines
        the cross-validation strategy to be used for splitting.

        Possible inputs for cv are:
          - None, to use the default 3-fold cross-validation,
          - integer, to specify the number of folds.
          - An object to be used as a cross-validation generator.
          - An iterable yielding train/test splits.

        For integer/None inputs, if ``y`` is binary or multiclass,
        :class:`StratifiedKFold` used. If the estimator is not a classifier
        or if ``y`` is neither binary nor multiclass, :class:`KFold` is
        used.

    shuffle (bool, optional): Used when do_cv is set to True. Determines
        whether to shuffle the training data before splitting using
        cross-validation. Default set to True.

    random_state (int :class:`RandomState`): Pseudo-random number generator
        state used for random sampling.

    curves (array-like): A listing of which curves should be plotted on the
        resulting plot. Defaults to `("micro", "each_class")`
        i.e. "micro" for micro-averaged curve

    ax (:class:`matplotlib.axes.Axes`, optional): The axes upon which to
        plot the learning curve. If None, the plot is drawn on a new set of
        axes.

    figsize (2-tuple, optional): Tuple denoting figure size of the plot
        e.g. (6, 6). Defaults to ``None``.

    cmap (string or :class:`matplotlib.colors.Colormap` instance, optional):
        Colormap used for plotting the projection. View Matplotlib Colormap
        documentation for available options.
        https://matplotlib.org/users/colormaps.html

    title_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "large".

    text_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "medium".

Returns:
    ax (:class:`matplotlib.axes.Axes`): The axes on which the plot was
        drawn.

Example:
        >>> nb = classifier_factory(GaussianNB())
        >>> nb.plot_precision_recall_curve(X, y, random_state=1)
        <matplotlib.axes._subplots.AxesSubplot object at 0x7fe967d64490>
        >>> plt.show()

    .. image:: _static/examples/plot_precision_recall_curve.png
       :align: center
       :alt: Precision Recall Curve
\end{DoxyVerb}
 

Definition at line \mbox{\hyperlink{classifiers_8py_source_l00470}{470}} of file \mbox{\hyperlink{classifiers_8py_source}{classifiers.\+py}}.

\Hypertarget{namespacescikitplot_1_1classifiers_a5b1de56edca4dcb6404139ede4488de1}\index{scikitplot.classifiers@{scikitplot.classifiers}!plot\_roc\_curve\_with\_cv@{plot\_roc\_curve\_with\_cv}}
\index{plot\_roc\_curve\_with\_cv@{plot\_roc\_curve\_with\_cv}!scikitplot.classifiers@{scikitplot.classifiers}}
\doxysubsubsection{\texorpdfstring{plot\_roc\_curve\_with\_cv()}{plot\_roc\_curve\_with\_cv()}}
{\footnotesize\ttfamily \label{namespacescikitplot_1_1classifiers_a5b1de56edca4dcb6404139ede4488de1} 
scikitplot.\+classifiers.\+plot\+\_\+roc\+\_\+curve\+\_\+with\+\_\+cv (\begin{DoxyParamCaption}\item[{}]{clf}{, }\item[{}]{X}{, }\item[{}]{y}{, }\item[{}]{title}{ = {\ttfamily \textquotesingle{}ROC~Curves\textquotesingle{}}, }\item[{}]{do\+\_\+cv}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{cv}{ = {\ttfamily None}, }\item[{}]{shuffle}{ = {\ttfamily \mbox{\hyperlink{classTrue}{True}}}, }\item[{}]{random\+\_\+state}{ = {\ttfamily None}, }\item[{}]{curves}{ = {\ttfamily (\textquotesingle{}micro\textquotesingle{},~\textquotesingle{}macro\textquotesingle{},~\textquotesingle{}each\+\_\+class\textquotesingle{})}, }\item[{}]{ax}{ = {\ttfamily None}, }\item[{}]{figsize}{ = {\ttfamily None}, }\item[{}]{cmap}{ = {\ttfamily \textquotesingle{}nipy\+\_\+spectral\textquotesingle{}}, }\item[{}]{title\+\_\+fontsize}{ = {\ttfamily "{}large"{}}, }\item[{}]{text\+\_\+fontsize}{ = {\ttfamily "{}medium"{}}}\end{DoxyParamCaption})}

\begin{DoxyVerb}Generates the ROC curves for a given classifier and dataset.

Args:
    clf: Classifier instance that implements ``fit`` and ``predict``
        methods.

    X (array-like, shape (n_samples, n_features)):
        Training vector, where n_samples is the number of samples and
        n_features is the number of features.

    y (array-like, shape (n_samples) or (n_samples, n_features)):
        Target relative to X for classification.

    title (string, optional): Title of the generated plot. Defaults to
        "ROC Curves".

    do_cv (bool, optional): If True, the classifier is cross-validated on
        the dataset using the cross-validation strategy in `cv` to generate
        the confusion matrix. If False, the confusion matrix is generated
        without training or cross-validating the classifier. This assumes
        that the classifier has already been called with its `fit` method
        beforehand.

    cv (int, cross-validation generator, iterable, optional): Determines
        the cross-validation strategy to be used for splitting.

        Possible inputs for cv are:
          - None, to use the default 3-fold cross-validation,
          - integer, to specify the number of folds.
          - An object to be used as a cross-validation generator.
          - An iterable yielding train/test splits.

        For integer/None inputs, if ``y`` is binary or multiclass,
        :class:`StratifiedKFold` used. If the estimator is not a classifier
        or if ``y`` is neither binary nor multiclass, :class:`KFold` is
        used.

    shuffle (bool, optional): Used when do_cv is set to True. Determines
        whether to shuffle the training data before splitting using
        cross-validation. Default set to True.

    random_state (int :class:`RandomState`): Pseudo-random number generator
        state used for random sampling.

    curves (array-like): A listing of which curves should be plotted on the
        resulting plot. Defaults to `("micro", "macro", "each_class")`
        i.e. "micro" for micro-averaged curve, "macro" for macro-averaged
        curve

    ax (:class:`matplotlib.axes.Axes`, optional): The axes upon which to
        plot the learning curve. If None, the plot is drawn on a new set of
        axes.

    figsize (2-tuple, optional): Tuple denoting figure size of the plot
        e.g. (6, 6). Defaults to ``None``.

    cmap (string or :class:`matplotlib.colors.Colormap` instance, optional):
        Colormap used for plotting the projection. View Matplotlib Colormap
        documentation for available options.
        https://matplotlib.org/users/colormaps.html

    title_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "large".

    text_fontsize (string or int, optional): Matplotlib-style fontsizes.
        Use e.g. "small", "medium", "large" or integer-values. Defaults to
        "medium".

Returns:
    ax (:class:`matplotlib.axes.Axes`): The axes on which the plot was
        drawn.

Example:
        >>> nb = classifier_factory(GaussianNB())
        >>> nb.plot_roc_curve(X, y, random_state=1)
        <matplotlib.axes._subplots.AxesSubplot object at 0x7fe967d64490>
        >>> plt.show()

    .. image:: _static/examples/plot_roc_curve.png
       :align: center
       :alt: ROC Curves
\end{DoxyVerb}
 

Definition at line \mbox{\hyperlink{classifiers_8py_source_l00222}{222}} of file \mbox{\hyperlink{classifiers_8py_source}{classifiers.\+py}}.

